#include "CancelAction.h"


bool CancelAction::Act()
{
	// send this ID to reastaurant class fn that cancel order from OV orders and send 
	// it to canceled list

	/*
	pRes->cancelOrder(orderType->getID());

	*/
}

